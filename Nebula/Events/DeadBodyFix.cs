namespace Nebula.Events;
/*
public class DeadBodyFix : LocalEvent{
    byte parentId;
    public DeadBodyFix(byte parentId) : base(0.02f) { this.parentId = parentId; }
    public override void OnTerminal(){
        try{
            RPCEvents.CleanDeadBody(parentId);
        }catch{}
    }
}
*/